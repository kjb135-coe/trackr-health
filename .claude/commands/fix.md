# Fix Issues

Fix TypeScript errors, lint issues, and common problems.

## Steps

1. Run `npx tsc --noEmit` to find TypeScript errors
2. Run linter if available
3. Fix all identified issues
4. Re-run checks to verify fixes

Do NOT ask for confirmation. Just fix everything.
