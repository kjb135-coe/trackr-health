# Start Development Server

Start the Expo development server.

## Steps

1. Check if a server is already running on port 8081
2. If running, inform user and optionally restart
3. Run `npx expo start` to start the dev server

For iOS simulator specifically: `npm run ios`

Do NOT ask for confirmation. Just start it.
